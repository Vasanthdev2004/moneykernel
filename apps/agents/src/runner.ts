// Strategy runner placeholder. Real proposal generation arrives in G4 (prd.md section 16).
// The runner talks to the kernel only through the authenticated agent HTTP API.
export const RUNNER_VERSION = "0.0.0-placeholder";
