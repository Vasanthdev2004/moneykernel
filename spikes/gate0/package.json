{
  "name": "@moneykernel/spike-gate0",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Gate 0 integration spike (prd.md section 2.4). Throwaway read-only MCP probe; not product runtime code.",
  "scripts": {
    "spike:login": "node mcp-spike.ts login",
    "spike:list": "node mcp-spike.ts list",
    "spike:call": "node mcp-spike.ts call",
    "typecheck": "tsc -p tsconfig.json",
    "test": "node --experimental-vm-modules --test *.test.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "zod": "4.5.4"
  },
  "devDependencies": {
    "@types/node": "26.4.1",
    "typescript": "7.0.2"
  }
}
